doctype html
html
    head
        link(rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.4/css/bulma.min.css")
        meta(name="viewport" content="width=device-width, initial-scale=1")
    body
        .column-is-one-third
            h1.is-size-1 Newsletter
            form(action="/newsletter" method="post")
                .field
                    label.label E-mail
                    .control
                        input(type="text" class="input" placeholder="user@mail.com")
                .field.is-grouped 
                    .control
                        button(type="submit", class="button is-link") Sign up!